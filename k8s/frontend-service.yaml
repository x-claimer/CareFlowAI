apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: careflowai
  labels:
    app: frontend
    tier: web
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: frontend
